# Zigbee Minimal RTOS Application


The Zigbee Minimal RTOS application is a bare minimum application Zigbee stack and cmsis-rtos.

The Zigbee Minimal RTOS application provides a simple interface to the user via the command line interface (CLI).

This application is non-compliant since it does not have all the necessary component and configurations of a fully functional zigbee application.
